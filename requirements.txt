# 运行时：Python 3.12 / 项目 .venv312。新装使用 requirements-lock-py312.txt 约束。
# mplfinance 当前验收版本为预发行版，必须显式 pin；不要对全体依赖启用 --pre。
pandas~=2.3.0
pyarrow~=23.0
numpy~=2.4.0
curl_cffi~=0.13
tushare~=1.4
openpyxl~=3.1.0
mplfinance==0.12.10b0
matplotlib~=3.10.0
fastapi~=0.110
uvicorn~=0.27
pydantic~=2.0
requests~=2.32.0
pyyaml>=6.0     # logic_platform 配置
lark>=1.1       # logic_platform DSL 解析器
joblib>=1.3     # logic_platform 预测模型（sklearn 配套）
scikit-learn>=1.5  # logic_platform 预测模型（HistGradientBoosting 等）
