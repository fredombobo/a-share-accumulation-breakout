# 专业回测结果契约纠错验收记录

## 1. 范围与结论

本次只修改 accumulation_breakout（AB-Screener，端口 8001）的专业研究回测，不操作 AETF/8000，不恢复纸面交易，也不改变正式晋级阈值。WP97 已完成：结果字段、最长持有语义、精确权益路径折叠、旧任务降级提示和真实历史报告均已通过验收。该结论只代表结果契约纠错完成，不代表策略已晋级或系统已达到机构/实盘就绪状态。

## 2. 已确认的历史问题

1. 旧任务 `probt-0503a30ccdef` 的 OOS 和 2 倍成本结果使用 `net_avg_return` / `net_total_return`，而结论器只读取 `portfolio_total_return`，导致“成本后不为正”和“未超过基线”两条错误阻断。
2. 页面将 `exit_window` 当作最长持有展示；实际撮合引擎中它是二次出货观察窗，真实最长持有一直由固定 30 日配置控制。
3. 名义排行榜重复展示收益路径完全相同的参数，造成独立证据数量被视觉放大。
4. WF 零成交窗口只显示空指标，没有明确说明该窗口不构成验证证据。
5. `sample_step > 1` 的结果没有在首屏明确标记为抽样研究。

## 3. 纠错口径

- 组合净收益按 `portfolio_total_return → net_total_return → net_avg_return` 顺序读取；缺失保持缺失，不补零。
- `max_hold_days` 为独立、版本化参数，范围 2 至 120 个交易日；旧 v1.2 请求兼容回填 30 日且保持旧参数 ID。
- `exit_window` 统一命名为“二次出货观察窗”，不再替代最长持有。
- 独立路径只能用完整 IS 与 OOS 组合权益 SHA-256 判断，不能用四舍五入后的收益/PF 猜测。
- 排行榜保留 IS 排序，不使用 OOS 二次选参；等效参数折叠只减少重复展示，不改变入选组合。
- 旧持久化任务不删除、不重写；纠正后的结论由新代码身份的新任务产生。

## 4. 自动化证据

- 首轮后端定向回归：29 passed；v1.4 路径覆盖纠错回归：9 passed。
- 首轮前端专业回测回归：7 passed；v1.4 路径覆盖界面回归：8 passed。
- 严格全量质量门：Ruff、Mypy（25 个源文件）、严格架构检查、994 个 Pytest、16 个 Vitest、前端生产构建全部 PASS。
- 真实 8001 浏览器验收：`BROWSER_PROFESSIONAL_RESULT_CONTRACT_OK`；覆盖桌面、390px、结果弹窗、说明书跳转、旧任务降级提示，控制台与网络错误为零。
- 服务身份：8001 构建指纹 `6134dcc60b03`，行情日期 `20260828`，`research_mode=full`，`LIVE_TRADING_ENABLED=false`。

## 5. 真实历史任务证据

### 5.1 v1.3 发现的二次缺陷

真实任务 `probt-1338dcb27ce6` 完成后，收益、成本、基线与 WF 结论已纠正，但路径证据仍错误显示不完整。根因是网格引擎会按最低成交数排除部分参数行，而结果层仍以 4,480 个名义组合要求每行都有完整 IS/OOS 权益哈希。该任务保持不可变，没有回填或重写。

### 5.2 v1.4 最终真实任务

- 任务：`probt-e0a5962dc8aa`，`DONE/100`。
- 结果契约：`professional-backtest-v1.4.0`；参数目录：`professional-grid-v1.3.0`。
- 代码身份：`6134dcc60b03`；数据身份：`2026-08-28T22:08:27+08:00`。
- 输入 SHA-256：`53df98fcdbd941cbd4f0e348060550d644aea609e1ec494e1bbcd2ea5d8c5055`。
- 参数空间：4,480 个名义组合；3,920 个完整 IS+OOS 路径合格组合；560 个因证据不完整排除；折叠后 280 条独立联合权益路径。
- 冻结股票池：600 只，SHA-256 `ec2385861f36ce2868145faebeda98a06244e338b6132939db191c082c0b5d01`。
- 参数空间 SHA-256：`db661a4ca3e8cc329dd2d88728046be42c27ca20d6ccb80d8690bb2461df32bc`。
- 报告 SHA-256：`13688f0d552910b0060ec3796ed3ba27936518fb80b0729af7d79319c1bc43b6`，以 UTF-8 独立复算一致。

v1.3 与 v1.4 在相同冻结数据和参数空间下产生完全相同的入选参数、收益指标、成本压力、基线、WF 明细以及 IS/OOS/成本压力权益曲线 SHA-256，说明 v1.4 只纠正证据披露，没有改变回测结果：

| 区间 | 净收益 | 净 PF | 最大回撤 | 净成交 |
|---|---:|---:|---:|---:|
| IS | +12.756808% | 1.40467011 | 12.563572% | 91 |
| OOS | +7.456158% | 1.29927997 | 6.739338% | 79 |
| OOS 2 倍成本 | +4.856007% | 1.18743338 | 6.995487% | 79 |

OOS 超过固定种子随机基线（-12.349335%）和 MA20/60 基线（-3.507778%），因此旧版的“成本后不为正”和“未超过双基线”两条阻断均不存在。真实阻断只有一条：`滚动窗口证据不完整`。WF1 测试 36 笔、PF 1.12071398；WF2 为 0 笔并明确标为无验证证据；WF3 测试 53 笔、PF 1.50664729。

最终研究结论保持 fail-closed：`EXPLORATORY_WEAK`、`candidate_eligible=false`、`can_claim_edge=false`。本次使用 `sample_step=10`，只能称为抽样研究；不得描述为逐日完整复验，也不得自动进入每日选股参数。

## 6. 最终验收结果

- PASS：同一份指标在摘要卡、图表、结论器和 Markdown 报告中一致。
- PASS：正收益 2 倍成本与超过双基线的 OOS 不再被错误阻断。
- PASS：最长持有独立进入成交、WF、基线和身份；二次出货观察窗不再冒充最长持有。
- PASS：新结果披露名义、路径合格、排除和独立路径数量，并在独立排行榜披露等效参数数。
- PASS：WF 零成交明确显示“0 笔（无验证证据）”。
- PASS：步长大于 1 明确显示抽样研究；正式逐日复验仍要求步长 1。
- PASS：全量质量门和真实 8001 API/浏览器验收通过，实盘开关保持 false。

WP97 验收结论：**完成**。策略研究结论：**不晋级，仍需解决 WF2 零成交并另行完成逐日复验**。
