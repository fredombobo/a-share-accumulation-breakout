# AB 研究批次：2026-09-19

这轮先复验已有机制，再继续上次中断的 432 组网格。全部为历史探索，
不改变每日选股参数、纸面账本或权威任务 `v2auth20260829k` 的 FAIL 结论。

## 预先冻结的任务顺序

1. `probt-e0a5962dc8aa` 的已选参数，在原 600 股股票池、当前来源完整窗口上重放。
   原 +7.46% 报告的 2022 年预热区间缺来源和不可变历史，不能原样复验；
   新任务明确为窗口更新诊断，不能称为同窗复现。
2. `probt-mom6m-v1-control`、`probt-mom6m-v1-momentum` 原股票池、原窗口、原知识截止时点重放。
3. `probt-dual-v1-control`、`probt-dual-v1-momentum` 同样冻结重放。
4. `probt-1acfad78443b` 的 600 股、432 组参数和窗口原样重跑。

所有任务使用修正后的 `research-portfolio-v2.2.1` / `v2.1.3`。
停牌日预定 OPEN 退出允许缺失参考价；真实报价和成交量仍由成交引擎检查，
不能成交则保留持仓，后续交易日再试。INTRADAY/CLOSE 仍要求有效参考价。

## 保留证据和停止条件

- 执行前保存请求、股票清单、窗口、参数空间、知识截止时点、代码与数据哈希。
- 每组 IS/OOS 完成后保存全部组合，包括亏损、无成交和重复权益路径；不只存前 100 名。
- 网格只按 IS 选参数；随后报告 OOS、滚动窗口、随机/均线基线、双倍成本和最大回撤。
- 已观察的 OOS 不充当新盲测；参数组合数与独立权益路径数分别报告。
- 代码或快照身份改变时不接受结果；不自动换股、改窗或以旧 PASS 替代 FAIL。
- 单批预计 1–2 小时，执行上限 2 小时；取消、超时或失败保留已完成分组。
- 只向既有研究表新增本批任务；不运行数据库迁移，不改行情或纸面账本。
- 记录生产纸面账本前后哈希；结果仅限 research_only，不自动晋级。

入口：`scripts/run_research_batch_20260919.py --prepare`，随后 `--execute`。
证据保存在受忽略的 `runtime/research/batch-20260919-v2/`。
v1 仅完成输入登记，因全量测试暴露两处旧模型版本断言而在执行前作废；
保留该次登记，修正断言后以 v2 重新冻结代码身份。
