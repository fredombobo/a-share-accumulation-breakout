# 每日选股、专业回测与 AI 证据评测验收报告

日期：2026-08-29  
产品：`accumulation_breakout` / AB-Screener / 端口 8001  
范围结论：**PASS**  
机构/实盘总闸门：**仍为 BLOCKED**，本报告不改变既有 R/O/S 结论。

## 1. 发布面与安全边界

- 前端仅保留每日选股、个股详情、专业回测；旧单参数实验室和纸面交易不恢复。
- 8001 OpenAPI 共 36 条路径，其中专业回测 7 条、AI 证据评测 2 条、`paper=0`、`lab=0`。
- `/paper` 在全新浏览器上下文中回到首页。
- 运行实例健康：`as_of=20260828`、`research_mode=full`、`LIVE_TRADING_ENABLED=false`。
- 没有新增券商适配器、订单或真实交易能力；历史账本和研究证据未删除。

## 2. 专业回测验收

- 参数目录共 13 项，支持固定值、范围步进、离散值；默认参数空间为 144 组。
- 默认横盘最长天数按 60–200、步长 20 展开；突破量比、止损、退出窗口均参与组合。
- 默认全市场预览冻结 600 只股票；半导体定向预览冻结 20 只，参数空间哈希与股票池 SHA-256 均返回。
- 动态预热按参数计算，实际预览为 265 日；完整窗口为 IS `20230801–20250731`、OOS `20250801–20260731`，含 3 个 WF 窗口。
- 当前行业分类只用于选择并冻结代码，页面和报告均披露其不是历史行业成员 PIT。
- 北交所代码 `920799.BJ` 被 `UNKNOWN_STOCK` 结构化拒绝；首版不冒充已支持不同交易规则的北交所股票。
- 大任务取消记录保持不可变：`probt-68ddf5737506` 与 `probt-48bd10a8e292` 均安全转为 `CANCELLED`，无活动任务残留。
- 最终真实闭环任务 `probt-d0b44dbb45b0`：
  - 1 组固定参数、29 只沪深候选、交易日采样间隔 10、动态预热 270 日；
  - 输入哈希 `96a4754e39539e1edbec5dc14b9204194e03e61abce6b46e030965d74e019417`；
  - `DONE/100`，完成 IS、OOS、WF、固定种子随机基线、MA20/60 基线和 2×成本压力；
  - 报告 SHA-256 `4ff135fc4bdef4be6a940bf9e18dd1021581e9ab34b212dce23996bf28934fc7`；
  - 诚实结论 `EXPLORATORY_WEAK`：IS 实际成交 30 笔、OOS 0 笔，WF 证据不完整，2×成本收益未保持为正；
  - `candidate_eligible=false`、`can_claim_edge=false`，没有改变生产策略或每日选股。
- 验收中补充了 IS/OOS 分片、组合会计估值心跳，以及组合会计逐日取消检查；页面把 `sample_step` 明确改名为“交易日采样间隔”。

## 3. AI 证据评测验收

- 每张候选卡和个股详情均提供“AI 评测”入口。
- 真实候选 `603587.SH` 返回本地结构化结论、2 条支持证据、2 条风险/缺口以及证据/信号时点。
- GET 评测只读，不调用模型、不修改扫描、信号或交易状态。
- 当前 DeepSeek 未配置时，能力字段返回 `available=false`，前端按钮直接显示“AI 文字解读未配置”并禁用；没有原始 JSON、延迟 503 或控制台错误。
- 本地证据评测不依赖外部模型，始终可用；外部模型结果只能解释已有证据。

## 4. 浏览器与质量证据

- 全新 Chromium 上下文：每日选股、B 池候选、个股 AI 面板、专业回测预览和 `/paper` 回首页均通过。
- 专业回测必须先预览，预览前“启动专业回测”禁用，预览后才启用。
- 390×844 视口 `scrollWidth=clientWidth=390`，无横向溢出。
- 验收页面 console/page/request errors 均为 0。
- 隔离 Playwright：5 项通过；前端 Vitest：3 文件 6 项通过。
- 严格质量门：Ruff PASS、Mypy 25 个源文件 PASS、strict architecture PASS、967 个离线 Pytest PASS、生产构建 PASS。
- ECharts 独立懒加载块约 642.5 kB，构建有体积提示但不阻断本范围验收。

浏览器截图位于 `runtime/acceptance/2026-08-29/`，仅作本地验收证据，不属于不可变研究结论。

## 5. 未宣称完成的事项

- 当前没有可晋级策略 edge；既有机构七闸门仍为 R/O FAIL、S INSUFFICIENT。
- 筹码插件 `chip_cost_concentration_v1` 只建立 PIT 扩展口，经济机制完成预登记前保持关闭。
- 当前分类不是历史行业成分 PIT；报告必须继续披露该限制。
- 外部 AI 模型未配置；本次 PASS 指本地证据评测和安全降级，不代表外部模型服务可用。
- 不支持纸面交易、北交所专业回测或任何真实下单。

## 6. 回滚

回滚发布路由、前端构建和本工作包提交即可。不得删除研究运行、失败/取消记录、历史账本或行情库；`LIVE_TRADING_ENABLED` 必须保持 `false`。
