# 小白使用手册（最少操作）

> 研究辅助工具，**不是投资建议**。

## 方式 A：丢给 AI Agent（最省事）

1. 打开 [PROMPT_FOR_AGENT.md](../PROMPT_FOR_AGENT.md)  
2. 把里面的提示词复制给任意 Agent，**只改成你的 Tushare Token**  
3. 等 Agent 说 `BOOTSTRAP_OK`，浏览器打开 http://127.0.0.1:8001/  
4. 点 **「扫描」**，看 **A 池**

你本人只需要：**提供 Token**。

## 方式 B：自己点 3 步

| 步骤 | 做什么 | 大约多久 |
|------|--------|----------|
| ① | 安装 Python 3.11+（勾选 Add to PATH） | 5 分钟，仅第一次 |
| ② | **双击 `一键启动.bat`**，按提示粘贴 Token | 首次建库 30～90 分钟；日常 2～10 分钟 |
| ③ | 浏览器打开后点 **「扫描」**，等结果 | 5～15 分钟 |

然后看 **A 池**（可关注的突破票）。**B 池**只是观察，别当主推。

停止：双击 **`停止.bat`**。

---

## 现在只用两个入口

- **每日选股**：同步数据、运行扫描、看 A 池，再点进个股查看证据。
- **纸面仿真**：用历史开盘价练习确认、成交、持仓和对账，不连接券商。

策略实验室、交互式回测台和机构控制台已经从日用界面移除。相关研究底座仍由维护者离线保留，
但未通过门禁的研究不会显示成可交易能力，也不会自动进入 A 池。

## 纸面交易：三步模拟

页面始终显示 **“纸面仿真，不会向券商下单”**。

1. 首次使用先创建纸面账户。填写的是**当前可用现金**，不是包含持仓的总资产。
2. 选择一个本地已有行情的历史开市日，输入六位股票代码和整手数量，点 **“检查并预览”**。
3. 核对决策日、成交日、开盘价、滑点、费用、预留现金和风控检查，再点 **“确认模拟订单”**。
4. 点击 **“按 YYYYMMDD 开盘模拟成交”**，直接查看成交数量、剩余现金、持仓变化和对账结果。

卖出不需要重新手输代码：在“当前持仓”行点 **“模拟卖出”**，系统会带入代码和可卖上限。若显示 **“T+1 暂不可卖”**，等份额可卖后再操作。

周末、节假日、缺行情、现金不足或仓位超限都会在页面显示“原因 + 怎么解决”；技术错误码折叠在“查看技术详情”中。

第一次不放心时点 **“第一次使用演练”**。演练读取本地历史行情和固定 10 万元演示资金，**不会写入你的纸面账户、订单、成交、现金或持仓**。

---

## 第一次用

1. 安装 [Python](https://www.python.org/downloads/)（安装时勾选 **Add python.exe to PATH**）
2. 解压/克隆本项目到任意文件夹
3. 双击 **`一键启动.bat`**
4. 窗口提示粘贴 **Tushare Token**（[tushare.pro](https://tushare.pro) 注册后在个人中心复制）
5. 等待「数据同步」跑完（第一次最久）
6. 自动打开网页：`http://127.0.0.1:8001/`
7. 点界面里的 **扫描**，喝口水等 5～15 分钟
8. 看 **A 池** 列表；点股票名可看 K 线

---

## 以后每天用

1. 双击 **`一键启动.bat`**（自动增量同步，约 2～10 分钟）
2. 网页里点 **扫描**
3. 看 A 池

---

## 重要注意（请看完）

1. **没有 Token 拉不到数据** —— 一键启动会引导你填；也可编辑项目里的 `.env` 文件。  
2. **A 池 vs B 池** —— 只认真看 A 池；B 池条件更松，仅观察。  
3. **A 池是空的？**  
   - 若显示「防守」环境：市场偏弱，系统故意不让新开仓，**正常**。  
   - 若数据日期很旧：再跑一次一键启动同步。  
4. **不要把 `.env` 和 Token 发给别人 / 传到公开网盘。**  
5. **扫描要时间** —— 全市场约 5000 只，多核大概 **5～15 分钟**，别反复狂点。  
6. **本工具不荐股、不保证收益**；止损、仓位自己定。  

---

## 耗时概念（心里有数就行）

| 动作 | 大约时间 |
|------|----------|
| 日常同步行情 | 2～10 分钟 |
| 第一次建数据库 | 30～90 分钟+ |
| 全市场扫描 | 5～15 分钟 |
| 打开网页 | 约半分钟 |

机器越快、网络越好，越靠区间下限。

---

## 进阶（可跳过）

```text
python sync_daily.py
python run_screener.py --top 15 --days 160 --workers 0
.\start_ui.ps1
```

开发前端：`web/frontend` 下 `npm run dev`（端口 3001）。
