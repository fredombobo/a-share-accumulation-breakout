# AB Screener 项目改进计划

> 日期：2026-08-03 ｜ 状态：**第一批已修复（本轮完成）**，P1 全部完成，P2 大部分完成，剩余为低优先级清理项
> 范围：`E:\CODEX\Stock_selection\accumulation_breakout\` 全项目

## ✅ 执行进度总览（2026-08-03 22:40 更新）

| 批次 | 项目 | 状态 |
|------|------|------|
| 直接修复 | 并发互斥 / 真取消 / 停止脚本 / 启动提示 / 缓存清理 / P0 路径穿越 | ✅ 完成（7 项，冒烟 ALL OK） |
| P1 | A1 财报去重口径 · A2 资金流分母 · A3 回测 N+1+入场 · A4 依赖 pin · A5 前端竞态 | ✅ 完成（5 项，均验证） |
| P2 | B1-B8、B10-B14（local_store 连接/upsert/路径、评分参数化、主题向量化、portfolio 原子写、charting 箱体区间、make_report 转义、audit_funnel 接口化、launcher 相对路径、app.py 弃用+JSON 缓存、tests 骨架） | ✅ 完成（14 项，unittest 14/14 + tsc 通过） |
| P2 剩余 | B9（Streamlit 完全废弃删除） | ✅ 完成（2026-08-03 22:44）：app.py 已删除，README/web-README 引用已清理，无代码残留引用 |

**验证记录**：全部改动文件 py_compile 通过；`out/_smoke_test_fixes.py` 回归 ALL OK；`python -m unittest discover -s tests -v` 14/14 通过；前端 `npx tsc --noEmit` 0 错误；A2 行为断言（净流入/双边成交额 = 5.88%）通过。

---

## 一、本轮已直接修复（7 项，已验证）

| # | 级别 | 问题 | 修复内容 | 验证 |
|---|------|------|----------|------|
| 1 | **P0 安全** | `web/backend_app.py` `_spa_fallback` 路径穿越，`GET /../.env` 可读出 TUSHARE_TOKEN | 拒绝含 `..` 的路径 + `resolve()` 后校验必须位于 dist 目录内 | 冒烟测试通过（原始与编码穿越均 404） |
| 2 | **P1 资源** | `POST /api/scan` 无并发互斥，连点可开多线程×≤16 worker 进程，CPU/内存打爆 | 已有 pending/running 任务时返回 **409**，附已有 task_id | 冒烟测试通过（第二次请求被拒） |
| 3 | **P1 资源** | 取消是假的：只置 `cancel_requested` 标志，无人检查，CPU 继续烧完 | `threading.Event` 真触发 → `run_scan`/`detect_many` 分片粒度检查 → `pool.shutdown(cancel_futures=True)` 释放进程池，防孤儿挂机 | 冒烟测试通过（事件置位 + 单进程取消路径） |
| 4 | **P1 资源** | `stop_ui.ps1` 只杀监听进程，扫描 worker 孤儿与 node 子进程杀不掉 | 按命令行匹配（backend_app.py/accumulation_breakout/frontend/vite）杀全部相关 python/node，`taskkill /T` 整树杀 | 脚本执行无错（当前无进程） |
| 5 | **P2 体验** | 残留进程占 8000 端口时启动失败无提示（本机已出现 Errno 10048） | `easy_start.py`/`bootstrap.py`：bind 失败且端口被占 → 明确提示「先运行 停止.bat」 | 逻辑已并入启动流程 |
| 6 | **P2 内存** | `_SCAN_TASKS` 只增不删、`_SECTOR_FLOW_CACHE` 无限增长 | 任务字典上限 20 条自动清理；板块缓存上限 6 条 | 冒烟测试通过（prune 后 20 条） |
| 7 | **P2 健壮性** | `easy_start` health 探测仅 1.5s，慢启动易误判重复拉起 | 探测延长至 4s | 代码审查 |

已修改文件：`web/backend_app.py`、`parallel_scan.py`、`run_screener.py`、`easy_start.py`、`bootstrap.py`、`stop_ui.ps1`
回归测试脚本：`out/_smoke_test_fixes.py`（可重复执行）

---

## 二、待办改进点（全项目审查结果）

### P1 — 建议优先（数据正确性 / 性能 / 依赖）

| # | 位置 | 问题 | 建议 | 工作量 |
|---|------|------|------|--------|
| A1 | `local_store.py` L321-326 | `sync_fina_for_codes` 缺失判定用 `(ts_code, ann_date)`，拉取后过滤用 `end_date`，二者几乎不等 → 每次全量重拉财报 | 统一按 `end_date` 判定缺失 | 0.5 天 |
| A2 | `scoring.py` `calc_fund_flow_strength` | moneyflow 表无 `amount` 列，分母回退「买入额合计」≈ 成交额一半 → ratio 系统性翻倍，放款资金流门槛 | 分子分母统一同源（买卖额口径）或改全市场口径 | 0.5 天 |
| A3 | `backtest_signals.py` | 双重 N+1：每采样日×每股 2 次 `load_daily`（100 日×400 股≈8 万次查询，跑数小时）；入场用采样日+1 而非突破日+1，收益口径偏 | 按 code 一次性加载全区间切片复用；入场锚定 `breakout_date` | 1 天 |
| A4 | `requirements.txt` | 缺 `streamlit`（app.py 依赖）、`pytest`；版本全 `>=` 无上限，2026 大版本升级可破坏 | 补全依赖并 pin `~=` 兼容区间 | 0.5 天 |
| A5 | `web/frontend/src/.../client.ts` | `request()` 无 AbortController/超时；Overview 切池时旧响应乱序覆盖新数据（竞态） | AbortController + 响应序号守卫 | 0.5 天 |

### P2 — 常规改进（按性价比排）

| # | 位置 | 问题 | 建议 | 工作量 |
|---|------|------|------|--------|
| B1 | `local_store.py` | `_connect()` 每次新建连接、`with` 不显式 close（依赖 GC）、每连接重复 PRAGMA；`distinct_dates` 全量查再切片 | 模块级连接复用/显式 close；SQL 层 LIMIT | 0.5 天 |
| B2 | `local_store.py` `_upsert` | df 仅含主键列时 `set_cols` 为空 → `DO UPDATE SET` 语法错误 | 空 set_cols 降级 `INSERT OR IGNORE` | 0.5 天 |
| B3 | `local_store.py` L302 | 硬编码 `E:\openclaw\stock_picker_cn` 注入 sys.path | 去掉或相对探测 | 0.5 天 |
| B4 | `scoring.py`/`pool_select.py` | 估值/市值区间硬编码（8-30、50-500）与 config 脱节；`build_master_score` 权重 0.55/0.25/0.20 魔法数 | 全部移入 config.py | 0.5 天 |
| B5 | `sector_themes.py` | `annotate_themes`/`apply_soft_theme_bonus` 逐行 iterrows 调 match_themes（5000 股慢）；L267 `chipish` 死代码；score_col 兜底 `df.columns[-1]` 可能错列 | vectorize/预计算映射；删死代码；显式列校验 | 0.5 天 |
| B6 | `portfolio.py` | `save_portfolio` 非原子写（write_text）+ 无锁 → 并发丢更新/写坏 | 临时文件 + `os.replace` + 线程锁 | 0.5 天 |
| B7 | `charting.py` | 箱体上下沿画满全图（应限箱体区间） | 图内限区间绘制 | 0.5 天 |
| B8 | `app.py` | `_gen_chart_on_demand` 用 `pickle.load` 读缓存（RCE 面） | 改 json/安全格式或移除 | 0.5 天 |
| B9 | `app.py`（Streamlit） | 与 FastAPI 后端双 UI 并存、逻辑重复维护、依赖 streamlit | 废弃 Streamlit 版，统一到 8000 单端口 | 1 天 |
| B10 | `make_report.py` | 假定 xlsx 必含全部列（KeyError 风险）；Markdown 单元格未转义 | 列存在性检查 + 转义 | 0.5 天 |
| B11 | `audit_funnel.py` | 硬编码 `runtime/stock_data.db` 路径；调私有 `store._connect()` | 走 LocalStore 公共接口 | 0.5 天 |
| B12 | `_write_launchers.py` | 硬编码绝对根路径，迁移后失效 | 相对路径生成 | 0.5 天 |
| B13 | 前端 | `flowDays` 切换连带重拉个股 flow；`scanCache` 存含全量 kline 的 OverviewResp 可能超配额静默失效；轮询无退避 | 分离请求、缓存瘦身、指数退避 | 1 天 |
| B14 | tests | 无 local_store/backend_app/scoring/backtest 单测；测试依赖外网；非 pytest、无 CI | 补核心模块单测（重点 `_upsert`/事务/WAL 并发）+ pytest + 简易 CI | 2 天 |

---

## 三、建议执行顺序

1. **第一批（防事故/数据正确）**：A1 → A2 → A3 → A5（合计约 2.5 天）
2. **第二批（工程化）**：A4 → B1 → B2 → B6 → B14 核心单测（约 3.5 天）
3. **第三批（清理）**：B9（废弃 Streamlit）→ B3/B11/B12 硬编码清理 → B4/B5/B7/B8/B10/B13（约 3 天）

## 四、备注

- 资源占用问题（并发互斥、真取消、停止脚本、孤儿进程）已全部在本轮修复，`out/_smoke_test_fixes.py` 可随时回归验证。
- 建议每次大改后跑一遍 `python out/_smoke_test_fixes.py` + `python -m py_compile` 相关模块。
