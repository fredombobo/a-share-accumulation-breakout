# HANDOFF-V2-P0-P3 — 机构级控制台 v2 交接（2026-08-18）

> 对应 runbook：`docs/superpowers/plans/2026-08-16-institutional-console-v2-agent-runbook.md` §7
> 本文档由集成 Agent 撰写，供下一个执行 Agent / 最终审计 Agent / 用户接手使用。

## 1. 身份

| 字段 | 内容 |
|---|---|
| Agent | integration-agent（单线推进，未启用多 worktree 并行） |
| 分支 | `codex/beginner-guided-ui` |
| 计划冻结 commit | `2b2520d`（v2 docs + tasks DAG） |
| 基线 commit | `c47aa26`（P0.1 真实基线重测） |
| 最新交付 commit | `cdd33e0`（P3） |
| 权威环境 | `.venv312`（Python 3.12.10，`C:\Users\13818\AppData\Local\Programs\Python\Python312\python.exe -m venv .venv312`） |
| 基线 manifest | `runtime/v2/baseline_manifest.json`（**identity `e6d403face964f3b`，git `b5a8346`（handoff 提交），pytest 508/508，BLOCKED [WORKTREE_DIRTY]**） |
| 用户文档 | `docs/STATUS.md`、`docs/RESEARCH-ROADMAP.md` —— 用户未提交修改，**全程未触碰，禁止覆盖** |

### 交付 commit 列表（P0–P3）

| commit | 内容 |
|---|---|
| `c47aa26` | P0.1 基线重测：capture_v2_baseline + manifest + venv312 |
| `cf17137` | P0.4 契约：错误码注册表/平台配置/七闸门/迁移注册表 |
| `4729074` | P0.2+P0.3：ENTRY registry 指纹 + 质量门禁（NO_REPLACE 清零/arch 检查/quality_gate.ps1） |
| `9f9bec5` | style：ui_trade_chart_flow import 排序 |
| `89a5697` | P1.1：PIT 五元组/历史表/适配器/回填断点续跑 |
| `8e68ef2` | P1.2：as-of 股票池 + instrument 规则（fail-closed） |
| `e0b2f3f` | P1.3：复权/公司行为账本/数据质量门禁 |
| `833e233` | P1.4：市场情报（只读） |
| `6dc8ee1` | P2.1：唯一执行领域核心（整数分账务） |
| `31d4df8` | P2.2：可成交语义 + 研究/纸面 parity |
| `457b9a6` | P2.3：执行血缘固化（M009 + v2:execution_lineage） |
| `3de8489` | fix：注册 v2:execution_lineage 迁移意图 |
| `cdd33e0` | P3：研究治理/正式统计/成本容量/晋级服务 |

## 2. 完成范围

- [x] **P0.1** 基线采集器 + manifest 身份（identity 可复算、敏感字段验证）
- [x] **P0.2** ENTRY V1 golden 冻结 + 注册表 + V2 定义（激活 V2 不改变 V1）
- [x] **P0.3** 质量门禁：生产代码 `INSERT OR REPLACE` 清零；`check_architecture.py` 白名单+`--strict`；`quality_gate.ps1` 一条命令；CI 增 Architecture 步骤
- [x] **P0.4** 错误码注册表（20 码）/resolved config + feature flags/LIVE 硬门/七闸门/迁移注册表（自完备 `_ensure_intents`）
- [x] **P1.1** PIT 五元组（revision/available_at/source/content_hash，+08:00）；append-only 历史表 + 清单；tushare adapter（仅根 `tushare_init`）；回填 checkpoint 断点续跑；`docs/DATA-DICTIONARY-PIT-V2.md`
- [x] **P1.2** 证券分类（stock/index/etf/fund/bond/bse）+ 生命周期 `[list_date, delist_date)`；`universe_asof`/`require_instrument` fail-closed；`optimizer.research_universe(as_of=...)`；`paper_trading/rules.require_rule` 严格路径
- [x] **P1.3** 公司行为 append-only 账本 + 状态投影 + 追加冲正；日结阻断（`CORPORATE_ACTION_PENDING`）；数据质量门禁（重复键/OHLC/负量额/覆盖率≥98%/持仓100%/源端比对；无 Token=INSUFFICIENT）
- [x] **P1.4** 市场情报只读：档案/时间线（PIT available_at）/宽度/来源状态；snapshot 指纹按 manifest 失效
- [x] **P2.1** 执行核心：整数分/微元账务（浮点输入拒绝）、费用逐项复算、tick/滑点钳制/参与率/FIFO/T+1、防重复成交；`costs.dual_run_observer` 写路径切换门；`docs/EXECUTION-MODEL-V2.md`
- [x] **P2.2** DAY 余量过期/收盘信号护栏/停牌不算撮合；研究/纸面同 fixture 成交 hash 一致
- [x] **P2.3** 执行血缘：`pt_fill` 增 fee_breakdown/版本/参与率/quote 时点/input hash（paper M009 + v2:execution_lineage 幂等对齐）
- [x] **P3.1** 实验 registry/trial ledger（失败/取消/拒绝同登记，核心字段不可变）+ 产物仓库（哈希防篡改）+ 迁移 `v2:research_governance`
- [x] **P3.2** 正式统计：CSCV-PBO（论文 rank 口径）/DSR（SR0=E[max]）/MinTRL/nested walk-forward（扩展窗无重叠、测试折只评一次）；`validation.v2_statistics_block` + trusted_run 报告集成（样本不足=INSUFFICIENT 不伪造）
- [x] **P3.3** 成本压力（1×/2×/3×）+ 容量模型（ADV20/退出天数/账户规模档）
- [x] **P3.4** 晋级服务双口径（ROBUST_PERSONAL_V2/STRICT_RESEARCH_V2，PASS 仅生成 CANDIDATE，绝不写 A 池/订单）+ artifact manifest 篡改检测

## 3. 明确未完成

- [ ] **用户文档合并**：`docs/STATUS.md`、`docs/RESEARCH-ROADMAP.md` 仍是用户未提交修改 —— 整体状态必须保持 `BLOCKED [WORKTREE_DIRTY]` 直到用户合并
- [ ] **P4 六形态策略插件**（`ab_screener/strategies/`、`regimes/`、`scan_funnel.py`、`signal_pipeline.py`、`signal_outcomes.py`、scan/signal repository）—— DAG 中 `V2-P4-STRATEGIES-SIGNALS` blocked
- [ ] **P5 风险约束**（组合暴露/VaR/CVaR/压力/唯一约束引擎；`paper_trading/orders.py|engine.py|settlement.py|rules.py` 的 WP50 集成）
- [ ] **P6 DAG/审计/告警/备份**；**P7 API 十进制字符串 + OpenAPI/typed UI**；**P8 总验收**
- [ ] **真实 517 万行 PIT 回填**：必须维护窗口 + 绝对路径副本（`scripts/backfill_pit_v2.py --preflight/--run/--coverage`），覆盖率/hash 100% 后才切 `V2_PIT_READ_ENABLED=true`
- [ ] **架构债务 4 项**（P5 拆分后 `--strict` 清零）：`web/backend_app.py` sqlite3×10+subprocess×1、`ab_screener/api/scan_router.py` sqlite3×2+subprocess×1
- [ ] **v2 写路径切换**：`costs.dual_run_observer().v2_ready_for_write_path` 当前 `False`；纸面/研究写路径仍 legacy-v1（P2.2 parity 完成后才允许切换）
- [ ] **API 金额/价格十进制字符串**（P2.3 验收项，落在 P7）
- [ ] **migration registry 接入 Web 启动**：`assert_schema_compatible` 已实现但尚未在 backend_app 启动时调用（P0.4 遗留，建议 P5/P7 接线）

## 4. 修改/新增文件

### added（本阶段）
```
ab_screener/domain/errors_v2.py, platform_config.py(application), readiness.py,
  entry_registry.py, entry_definition_v2.py, data_point.py, instrument.py,
  execution/{__init__,models,fees,market_rules,fill_model,settlement_rules,order_semantics,dual_run}.py
ab_screener/data/{pit_repository,pit_writer,instrument_repository,corporate_action_repository,
  research_artifact_repository,intelligence_repository,migration_registry,schema_check}.py
ab_screener/data/adapters/{__init__,tushare_pit}.py
ab_screener/data/migration_intents/{__init__,pit_history_v2,instrument_history_v2,
  corporate_actions_v2,execution_lineage_v2,research_governance_v2}.py
ab_screener/application/{pit_backfill,corporate_action_service,data_quality}.py
ab_screener/intelligence/{__init__,catalog,timeline,events,breadth,quality}.py
ab_screener/research/{registry,trial_ledger,cscv,deflated_sharpe,min_track_record,
  nested_walkforward,cost_stress,capacity,promotion_v2,artifact_manifest}.py
scripts/{check_architecture,quality_gate,capture_v2_baseline,migrate_v2,backfill_pit_v2}.py
tests/test_architecture_boundaries.py, test_entry_definition_v1_golden.py, test_entry_definition_v2.py,
  test_pit_repository.py, test_pit_writer.py, test_pit_backfill_resume.py, test_instrument_universe_asof.py,
  test_optimizer_asof_universe.py, test_adjustment_asof.py, test_data_quality_v2.py, test_intelligence_catalog.py,
  test_market_breadth.py, test_event_timeline_pit.py, test_execution_core_v2.py, test_execution_money_exactness.py,
  test_execution_zero_partial_fill.py, test_execution_temporal_market_rules.py, test_execution_backtest_paper_parity.py,
  test_execution_lineage.py, test_experiment_registration.py, test_trial_ledger.py, test_cscv_pbo.py,
  test_deflated_sharpe.py, test_min_track_record.py, test_nested_walkforward_v2.py, test_cost_stress.py,
  test_capacity_model.py, test_promotion_gate_v2.py, test_research_artifact_tamper.py, test_v2_statistics_block.py
tests/fixtures/{entry_v1_golden.json, universe_lifecycle.csv}
docs/{ENTRY-DEFINITION-V2,API-CONTRACT-V2,ERROR-CODES-V2,DATA-DICTIONARY-PIT-V2,EXECUTION-MODEL-V2,ACCEPTANCE-V2-P0}.md
docs/ADR/{ADR-020-v2-module-boundaries,ADR-021-v2-readiness-gates}.md
```

### modified（本阶段）
```
local_store.py（NO_REPLACE→ON CONFLICT、ClassVar PK 表）、paper_trading/{cal,settlement,rules,engine,migrations,schema,real_data_gate}.py、
ab_screener/domain/{costs,__init__}.py、ab_screener/research/{validation,trusted_run}.py、
optimizer.py、backtest_signals.py、migration_registry.py、scripts/migrate_v2.py、
.github/workflows/ci.yml、.gitignore、tasks/implementation_state.yaml、tests/{test_migrations,test_cal_rules,test_architecture_boundaries}.py
```

### shared hotspot touched
- `web/backend_app.py`：**未改动**（架构债务白名单记录，P5 拆分）
- `ab_screener/data/migration_registry.py`：集成 Agent（schema steward 职责）—— `_ensure_intents` 自完备
- `paper_trading/migrations.py`：M009（执行血缘）由集成 Agent 按 WP50 授权添加

## 5. 公共契约

- **金额/价格**：金额一律整数「分」，价格整数微元（1 元 = 1_000_000 micro = 100 分）；浮点账务输入拒绝（`execution.models.MoneyError`）
- **时间**：统一 `+08:00`；`available_at`（数据可用时刻）/`decision_at`（决策时点）语义在 `data_point.normalize_ts`/`select_asof`
- **ENTRY**：V1 `A_POOL_STRICT_NEXT_OPEN_V1` 为生产候选（golden fixture 锁定）；V2 只读；报告必须带 `entry_definition_id` + `entry_semantic_hash`，hash 漂移拒绝生成
- **错误码**：`ab_screener/domain/errors_v2.py` 20 码冻结；`V2Error.to_envelope`
- **迁移**：namespace/string ID（paper/core/logic/v2）；`schema_migrations_v2` 按 id+checksum；DDL 唯一入口 `scripts/migrate_v2.py --db <绝对副本> --plan|--apply`；Web 启动只断言不 DDL
- **flags**：`configs/platform_v2.yaml`（8 flags，`LIVE_TRADING_ENABLED=true` 必须启动失败）
- **只追加原则**：账本/证据（历史表、corporate_actions、trials、manifests）append-only（触发器兜底）；可变投影仅 `ON CONFLICT DO UPDATE` 白名单字段

## 6. 迁移清单（注册表现状）

```
v2:pit_history            （6 张 *_history 表 + raw_ingest_manifests + pit_backfill_checkpoints）
v2:instrument_rules       （instrument_universe_rules + instrument_lifecycle_history，依赖 pit_history）
v2:corporate_actions      （corporate_actions 账本 + corporate_action_status 投影，依赖 pit_history）
v2:execution_lineage      （pt_fill 血缘列，与 paper M009 幂等对齐）
v2:research_governance    （experiment_registrations/research_trials/research_artifacts/promotion_decisions，依赖 pit_history）
```
冒烟证据：`scripts/migrate_v2.py --db <副本> --plan` 按依赖序显示待应用；`--apply` 应用后 `--plan` 为空。

## 7. 测试证据

- 全量离线门禁：**511 passed / 1 skipped**（`pytest tests/ -q -k "not browser"`，venv312）
- ruff：`ruff check . --exclude web/frontend/node_modules` → All checks passed
- mypy（CI 范围 + 新增模块）→ Success
- 基线 manifest：`runtime/v2/baseline_manifest.json`（**identity `e6d403face964f3b`，git `b5a8346`，pytest 508/508，BLOCKED [WORKTREE_DIRTY]**）
- 专项：`tests/test_architecture_boundaries.py` 5/5；`tests/test_v2_baseline_manifest.py` 3 passed+1 skip；迁移冒烟通过
- 未使用真实 Token；未修改 runtime 账本（回填/迁移均在临时副本上冒烟）

## 8. 风险与限制（诚实声明）

1. **整体 BLOCKED**：仅因 WORKTREE_DIRTY（用户 STATUS/ROADMAP 未合并），合并后重跑基线可转 OK
2. **架构债务 4 项**：backend_app/scan_router 直接 import sqlite3/subprocess（P5 拆分后 `--strict` 清零）
3. **写路径未切换**：纸面成交仍 legacy-v1（`cost_version='legacy-v1'`）；v2 执行核心已就绪但需 P2.2 parity 完成后再接管
4. **真实回填未执行**：517 万行 PIT 数据 + instrument 规则回填需要维护窗口与 Token（仓库只交付契约/实现/离线测试）
5. **fina_indicator 适配器参数**（period 语义）在真实维护窗口首次运行时校验
6. **migration registry 未接入 Web 启动**（assert_schema_compatible 已实现未接线）
7. **API 十进制字符串**（P2.3 验收）落在 P7
8. 基线 pytest 口径随测试演进（335 → 372 → 405 → 419 → 428 → 445 → 458 → 511 累计），以最新 manifest 为准

## 9. 下一步集成说明（P4）

- 上游依赖已齐：PIT（P1.1）、instrument（P1.2）、执行身份（P0.2/P2）、研究治理（P3）
- **P4.1 策略插件契约**：`ab_screener/strategies/`（contracts/registry/dsl_adapter/六个 v1 插件）+ `ab_screener/regimes/` + `configs/strategies/*.yaml` + `configs/regimes/*.yaml`
- 共享热点：`run_screener.py` 唯一 owner（WP40）；插件 `ACTIVE_FOR_A_POOL` 集成必须等 WP30（已完）
- `tasks/implementation_state.yaml`：`V2-P4-STRATEGIES-SIGNALS` 当前 blocked，接手时置 ready 并实现

## 10. 回滚

- **feature flags**：`V2_PIT_READ_ENABLED` 等 8 flags 全 false（`platform_v2.yaml`）；旧路径不产生 v2 候选
- **数据保留**：历史表/checkpoint/迁移记录保留不删；`runtime/` 不入库
- **账本/证据**：历史成交不重写；异常时停止新撮合以 model version 区分；现金流水不回滚
- **用户文档**：STATUS/ROADMAP 保持用户版本，回滚任何尝试覆盖它们的变更

## 11. 结论

**状态：READY_FOR_INTEGRATION（P0–P3）**；整体项目状态 `BLOCKED [WORKTREE_DIRTY]`（预期，待用户合并文档）。
下一任务：P4-STRATEGIES-SIGNALS。接手第一步：`git status` 确认仅有 STATUS/ROADMAP 两文件 dirty → 重跑 `scripts/capture_v2_baseline.py` 刷新基线 → 读 `tasks/implementation_state.yaml` 与 P4 计划节。
