# 最终验收报告（2026-08-08）

仓库：`E:\CODEX\Stock_selection\accumulation_breakout`

## 结论

纸面交易、扫描审计、数据时点、独立真实数据门禁和前端工作台通过功能验收。系统没有券商下单能力，`LIVE_TRADING_ENABLED=false`。

历史数据已经满足完整研究窗口，但这只表示“可以验证 edge”，不表示已经证明策略 edge；策略晋级仍必须通过净成本 OOS、走步和基线门禁。

## 最终证据

| 项目 | 结果 |
|---|---|
| Pytest | `175 passed` |
| Ruff | `All checks passed` |
| Mypy | `40 source files` 通过 |
| 前端构建 | 通过；最大 ECharts chunk `687.33KB` |
| SQLite | `quick_check=ok`，外键差异 `0`，schema `10` |
| 日线 | `5,145,184` 行，`969` 个交易日，`20220809~20260807` |
| 风险基准 | `000300.SH` 共 `969` 日，最新 `20260807` |
| 真实数据门禁 | PASS；100 个样本对，0 差异，公司行为权限通过 |
| 真实门禁报告 | `runtime/gates/final_acceptance_20260808/real_data_gate_20260808_123234.json` |
| 门禁代码版本 | `35fc69aa6fa9` |
| 门禁报告 SHA-256 | `fa1ec79901a021d583c792daaacc7dd92a2cd599e53c306621db9c783500a580` |
| 完整热扫描 | `116.506s`；A=2、B=30；审计 SHA-256 均为 64 位 |
| 扫描可复现 | 两次全量扫描业务结果哈希一致：`616728c3b34145d4e11e14ecf6731e8410194af32437880e01e07a8e0d8106a6` |
| 总览 API | 32 项、248,164B；冷 716ms、热 29ms |
| 纸面对账 | 未解决项 `0`；负现金 `0`；负持仓批次 `0` |
| UI | 桌面/390px 通过；控制台错误 `0`；失败请求 `0`；键盘焦点可见 |

## 本轮纠错

- 将扫描成功状态和不可变运行审计改为同一个 `BEGIN IMMEDIATE` 事务；取消请求优先。
- 扫描审计记录完整输入、结果和数据版本 SHA-256，并保存最终候选和漏斗。
- 结果哈希排除持久化时间，固定数据重复运行保持一致。
- 自动识别旧版“成功但无运行审计”的孤儿；不删除历史，追加 `INVALID_ORPHAN` 异常记录。
- 历史扩容到 969 个交易日，补齐日线中间缺口，并把资金流缺口检查限定在请求窗口。
- 沪深 300 使用独立 `index_daily` 同步；缺失或过期时运行时禁止开新仓。
- 草稿使用已固化的 PIT 信号状态；确认和撮合再复核最新市场环境。
- Parquet 缓存由 SQLite 触发器自动失效，不再存在中间日期更新仍命中旧缓存的问题。
- 回测候选统一按净成本指标晋级，修复次日开盘执行索引，前端不再以 gross 指标兜底。
- 完整扫描复用信号检测结果，移除关键路径外部财务请求；热扫描进入 120 秒目标。

## HTTP 与账本验收

- 当前实例 `/api/health`、40 条 OpenAPI 路径、总览、纸面账户、Dashboard、门禁、对账和扫描运行查询均返回 200。
- 当前纸面账户现金和权益均为 10,000 元，尚无持仓；这不影响下单、撮合、T+1、费用、日结和异常对账测试覆盖。
- 最新有效扫描运行：`6791c32cde70`，状态 `SUCCEEDED`，研究模式 `full`。
- 旧竞态产生的一条历史异常保留为 `INVALID_ORPHAN`，当前 `SUCCEEDED` 且无审计的任务数为 0。

## 已知非阻断债务

- `web/backend_app.py` 和 `run_screener.py` 仍偏大；交易域和扫描审计已模块化，但兼容路由壳的进一步拆分仍属于 P1 维护性工作。
- 全量冷扫描在缓存被完整失效后实测 `135.897s`；热扫描为 `116.506s`。日常同步应预热缓存，后续可继续降低冷启动成本。
- Eventlet、Starlette/FastAPI 依赖产生弃用警告，但本轮无运行错误；升级依赖前应单独回归。
- 目标仓库没有 `tasks/backlog.yaml` 或 `tasks/implementation_state.yaml`，因此无法按该文件体系写回任务状态；本报告作为本仓库验收状态来源。

## 回滚

- 设置 `PAPER_TRADING_ENABLED=false` 可关闭纸面模块和调度。
- `LIVE_TRADING_ENABLED` 必须保持 `false`。
- 数据库迁移仅追加表、列和触发器；账本只允许追加冲正，不删除历史流水。
- 备份位于 `runtime/backups/`；旧 `portfolio.json` 未被修改。
