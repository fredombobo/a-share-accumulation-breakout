# WP96 参数检查弹窗、真实结果图谱与界面精修验收

验收时间：2026-08-30 14:41 Asia/Shanghai  
产品：accumulation_breakout / AB-Screener  
发布地址：http://127.0.0.1:8001/  
结论：PASS

## 1. 已交付结果

- 参数空间检查成功后弹出明确结果，展示有效组合、冻结股票、预热长度、研究范围和耗时判断。
- 参数空间检查失败后弹出人话错误及返回修改步骤。
- 弹窗使用原生模态语义，支持初始焦点、Escape 关闭、焦点回归和背景隔离。
- 弹窗不启动回测；关闭后页面内冻结预览仍保留，参数变化会使旧预览失效。
- 完成结果新增五类图谱：净收益、Profit Factor、参数风险收益、排行榜 OOS 收益、WF 窗口稳定性。
- 入选参数由原始 JSON 改为人类可读摘要，原始参数、参数 ID 和输入哈希保留在折叠技术详情。
- 深浅主题、结果卡片、边界提示、间距和移动端层级完成保留式精修。
- 站内使用说明和小白手册已同步弹窗、五类图表及缺失数据口径。

## 2. 数据真实性检查

- 收益只读取 `portfolio_total_return`、`net_total_return` 或已在研究报告中定义为窗口组合净总收益的兼容字段 `net_avg_return`。
- 最大回撤只读取组合回撤或净回撤字段。
- `null`、空字符串、无穷值和非法数字均保持缺失，不转换为 0。
- 风险收益图不使用 OOS 重新排序；Top 图保持 IS 选参后的原排行榜顺序。
- WF 图只读取 `wf_detail` 的真实训练和测试 PF。
- 接口没有逐日净值序列时明确不绘制净值曲线，不生成插值或模拟曲线。
- 旧弱证据任务 `probt-d0b44dbb45b0` 只有两类指标足够绘图，因此真实页面绘制 2 张图，其余显示空状态；这符合 fail-closed 口径。

## 3. 自动化证据

- Ruff：PASS。
- Mypy：25 个配置源文件 PASS。
- 严格架构检查：PASS。
- 离线 Pytest：988 passed in 247.17s。
- Vitest：4 files / 15 tests PASS。
- Playwright：10/10 PASS，覆盖弹窗、Escape、五类图谱、390px 和现有核心流程。
- TypeScript 与 Vite 生产构建：PASS。
- `git diff --check`：PASS，仅显示既有 Windows 行尾提醒。

## 4. 真实 8001 验收

- `/api/health`：ok。
- `/api/v2/platform/status`：`product=accumulation_breakout`、`default_port=8001`。
- `LIVE_TRADING_ENABLED=false`。
- 深色桌面、浅色桌面和 390px 均无横向溢出，浏览器 console/page error 为 0。
- 参数检查真实预览返回 432 组、600 只股票、265 日预热；弹窗显示正确且 Escape 可关闭。
- 历史真实结果页按实际字段绘图且缺失项不补零。
- 本轮没有重启或停止服务。真实任务 `probt-0503a30ccdef` 从 12% 持续推进至 19%，仍处于 GRID 阶段。

## 5. 视觉审视结论

采用“克制、可信、数据优先”的个人金融研究工作台语言，保留现有路由、导航、字段顺序和双主题。没有引入炫技型大屏、装饰性渐变、新 UI 依赖或高强度动画。主要改善集中在操作反馈、结果可读性、状态层级和图表信息效率。

## 6. 未改变边界

- 未改变专业回测算法、成本、IS/OOS/WF、基线或晋级口径。
- 未修改数据库、扫描档案、参数档案或生产策略。
- 未恢复纸面仿真、订单或券商能力。
- 图表可视化不改变研究结论，FAIL 和证据不足继续优先显示。

## 7. 回滚

本轮没有数据库迁移。回滚前端与文档提交即可恢复旧表现层，不会影响持久化任务、研究结果或正在运行的回测。
