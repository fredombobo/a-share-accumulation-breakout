# 回测参数与今日选股闭环验收

日期：2026-08-30  
产品：`accumulation_breakout` / AB-Screener / 8001  
结论：工程验收通过；正式研究/机构总闸门结论不变，仍为 `BLOCKED`

## 已交付

- 专业回测结果新增“用于今日选股”证据门禁与人工启用入口。
- 新增不可变策略参数档案仓储，保存来源任务、参数、代码/数据身份、证据摘要和配置哈希。
- `GET /api/backtest/profile` 展示当前档案和历史；`POST /profile/activate` 人工启用；`POST /profile/reset` 恢复内置默认。
- 只有已完成、`EXPLORATORY_PROMISING`、代码及数据身份有效的专业网格结果可以启用；未确认、弱结论和过期身份全部结构化阻断。
- 扫描启动时冻结精确档案。A 池严格检测、扫描子进程结果和 `scan_runs.strategy_snapshot_json` 共用同一快照及哈希。
- A 池共享横盘、突破、双重量能和结构参数；止损比例同步到候选操作参考。B 池继续固定宽松观察规则。
- 首页显示当前档案、关键参数、哈希、所需回看窗口和恢复默认入口；回测页显示可启用/不可启用原因。
- 站内说明书新增“回测参数用于选股”完整流程，并解释资金、基本面、市场环境等额外日常门禁。
- 不完整或旧接口响应在前端失败关闭，不再造成页面崩溃。

## 自动化证据

最终干净代码身份运行 `scripts/quality_gate.ps1 -Strict`：

- Ruff：PASS
- Mypy：25 个受控源文件 PASS；新增公共模块另行定向 Mypy PASS
- 严格架构检查：PASS
- Pytest：`979 passed in 196.05s`
- Vitest：`11 passed`
- TypeScript + Vite 生产构建：PASS

新增测试覆盖：

- 人工确认、幂等启用、恢复默认和历史保留；
- 弱结论、代码过期和数据过期 fail-closed；
- 回测信号参数不变地传入严格检测；
- 自定义档案冻结到扫描结果和池报告；
- 子进程命令必须携带冻结档案文件；
- 合格结果仍需用户二次确认。

Playwright：`8 passed`，覆盖发布路由、分类、预览前置、全局进度、键盘、390px 和档案兼容响应。

## 真实 8001 验收

- 运行实例代码版本：`da6d55509bb1`
- OpenAPI 已发布 profile 读取、启用和恢复三个接口。
- 首页显示：系统默认，横盘 20–125 日、突破量比 1.6、哈希 `1e11cf43003bd787`。
- `/backtest` 与 `/guide` 正常；390px 无横向溢出；浏览器控制台错误为零。
- 生产 `strategy_profiles` 历史仍为 0，验收没有替用户启用参数或写入档案。
- 最近完成专业回测 `probt-d0b44dbb45b0` 为 `EXPLORATORY_WEAK`，接口正确返回 `can_activate=false`，主要阻断为证据未通过和代码身份过期。
- `LIVE_TRADING_ENABLED=false`；纸面和真实交易能力均未恢复。

## 用户闭环

1. 在专业回测冻结股票池和参数空间并运行。
2. 只有结果页出现“证据门槛通过，可人工启用”时，确认启用。
3. 回首页核对参数版本和哈希，再启动今日扫描。
4. A 池使用该技术参数，日常额外门禁继续过滤；B 池仍只观察。
5. 需要时点“恢复系统默认”，旧档案和旧扫描审计不会删除。

## 回滚

业务回滚优先调用恢复默认接口。代码回滚到实施前提交时无需删除 `strategy_profiles`；历史记录保持只读，后续扫描恢复内置参数。

## 提交

- `5fa7ef4`：计划与验收边界
- `96956f1`：参数档案、扫描冻结链路与前端闭环
- `3fb5dc8`：不完整档案响应 fail-closed
