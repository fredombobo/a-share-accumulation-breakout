# CLOSERS-E2 Handoff

## 身份
- Agent: 实现 Agent（WorkBuddy · SeniorDeveloper）
- 基线 commit: `2c04962`（origin/main，PR #1 astock 桥已合）
- 交付 commit: `8224daf`（E2 第4步）+ 后续第5步收口 commit
- 契约版本: PERSONAL-INSTITUTIONAL-CLOSERS-2026-08-21
- 波次: E2（G2 拆 `web/backend_app.py`）

## 环境
- Python 可执行文件: `.venv312\Scripts\python.exe`（sys.version 3.12.10）
  - 注：本机 `pyvenv.cfg` 的 `home` 原指向已失效路径，已修正为
    `E:\C_Drive_Moved_2026-06-03\AppData_Junctions\AppData\Local\Programs\Python\Python312`
- git: PortableGit，分支 `closers-g2-split`（**单层分支名**）
  - 注：git 2.55.0.windows.3 存在「带子目录分支 ref（`codex/xxx`）写入失败」的 bug，
    单层分支名正常。后续分支命名禁用 `codex/` 前缀。

## 完成范围
- [x] **第1步** `ab_screener/api/legacy_state.py` — 集中全部共享状态（store/缓存/扫描/Lab/同步/回测任务与锁 + `_paper_enabled`）
- [x] **第2步** `ab_screener/api/routers/legacy_misc.py` — 只读杂项 7 路由（health/setup-status/manifests/today/release-readiness/kline）
- [x] **第3步** `legacy_market.py` 市场数据 7 路由（overview/portfolio/stock/sector-flow/money-heatmap/stock-flow + 6 辅助函数）；`legacy_scan.py` 扫描 5 路由（scan worker + ScanRequest）
- [x] **第4步** `legacy_paper.py` 纸面 20 路由；`legacy_lab.py` 实验室 12 路由；`legacy_sync.py` 同步 2 路由；`legacy_backtest.py` 回测 3 路由
- [x] **第5步** 宿主收口：`backend_app.py` 从 3014 行 → 309 行，仅保留装配/中间件/SPA 托管/自动日终调度/启动

## 明确未完成
- [ ] E3（G3 根脚本迁包：signals/local_store/run_screener 迁 ab_screener）
- [ ] E4（G4 回测/扫描性能）
- [ ] E5 CI 严格门（quality_gate.ps1 -Strict 全量跑通）未在本波验证

## 修改文件
- added:
  - `ab_screener/api/legacy_state.py`
  - `ab_screener/api/routers/legacy_misc.py`
  - `ab_screener/api/routers/legacy_market.py`
  - `ab_screener/api/routers/legacy_scan.py`
  - `ab_screener/api/routers/legacy_paper.py`
  - `ab_screener/api/routers/legacy_lab.py`
  - `ab_screener/api/routers/legacy_sync.py`
  - `ab_screener/api/routers/legacy_backtest.py`
- modified:
  - `web/backend_app.py`（3014 → 309 行，只装配）
  - `tests/test_money_heatmap.py`、`tests/test_scan_progress_io.py`、
    `tests/test_lab_task_recovery.py`、`tests/test_paper_api_acceptance.py`、
    `tests/test_dev_port_isolation.py`（import 路径随拆分更新）
- 是否改 platform_v2.yaml: no
- 是否改 STATUS.md: no
- shared hotspot: backend_app（本波核心热点，已完成拆分）

## 测试证据
- 精确命令与退出码:

```powershell
& .\.venv312\Scripts\python.exe scripts\check_architecture.py --strict   # exit 0
& .\.venv312\Scripts\python.exe -m pytest tests\ -q -k "not browser"      # 658 passed, 2 failed
```

- 2 个失败已处置：
  - `test_daily_run_manifest.py::test_manifest_api_is_read_only_and_returns_latest`
    —— monkeypatch 目标随拆分更新到 `legacy_misc._DB`，已修复（复跑 5 passed，commit `3c22d64`）。
  - `test_v2_baseline_manifest.py::test_identity_stable_across_runs`
    —— `capture_v2_baseline.py` 超时 120s（脚本不 import backend_app/legacy，只读 12.8GB 大 DB 的 COUNT + 前端 assets hash，
    本环境需 >300s）。**既有环境超时，非 E2 引入**。
- 定向回归（拆分涉及的路由 + 契约 + 架构 + 端口隔离 + 幂等）: 40+ passed。

## 产物证据
- 是否使用真实 Token: no
- 是否修改 runtime 账本: no
- 是否写生产 DB PIT: no
- daily MAX: 20260818（未变）
- 旗标打印: LIVE_TRADING_ENABLED=False；除 V2_EXECUTION_DUAL_RUN_ENABLED 外生产项全 False

## 闸门自测（实现侧，非正式验收）
- G2-1 宿主只装配: PASS（backend_app 无业务路由函数，仅 SPA/中间件/调度）
- G2-2 无重复 path: PASS（OpenAPI 100 path，test_openapi_contract_v2 绿）
- G2-3 HTTP 契约不变: PASS（57 个 legacy 路由 + v2 路由全部保留，OpenAPI path 总数 100 不变）
- G2-4 离线测试: 见测试证据（全量 -k "not browser"）
- G2-5 架构: PASS（check_architecture --strict exit 0）
- G2-6 行为: PASS（扫描仍子进程+进度+取消落库，lab_optimize 经函数内 import 引用 _running_task_id）

## 结论
- READY_FOR_REVIEW
- 建议总状态: BLOCKED（D/O/R 闸门仍 INSUFFICIENT，非本波范围）
