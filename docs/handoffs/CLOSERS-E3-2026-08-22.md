# CLOSERS-E3 Handoff

## 身份
- Agent: 实现 Agent（WorkBuddy · SeniorDeveloper）
- 基线 commit: `a482050`（E2 完成点）
- 交付 commit: `c8800fa`
- 契约版本: PERSONAL-INSTITUTIONAL-CLOSERS-2026-08-21
- 波次: E3（G3 根脚本迁包）

## 环境
- Python: `.venv312\Scripts\python.exe`（3.12.10）
- git: 分支 `closers-g2-split`（单层分支名，禁用 `codex/` 前缀）

## 完成范围
- [x] `signals` / `scoring` / `local_store` / `run_screener` / `data_fetch` / `market_regime`
      6 个根脚本迁至 `ab_screener/` 同名模块（正式入口）
- [x] 根文件改为薄 re-export（`import ab_screener.x as _m` + `from ab_screener.x import *`
      + `globals().update(...)` 转发下划线私有名，兼容旧导入路径）
- [x] `ab_screener/local_store.py` 的 `_DB_DIR` 由 `os.path.dirname(__file__)` 改为
      `Path(__file__).resolve().parents[1]`（迁移后路径基准修正为项目根）
- [x] 2 个测试的 monkeypatch 目标改为 `ab_screener.local_store`（re-export 值拷贝问题）

## 明确未完成
- [ ] `config` / `charting` / `tushare_init` 等根脚本未迁（作为依赖保留在根，后续可迁）

## 修改文件
- added: `ab_screener/{signals,scoring,local_store,run_screener,data_fetch,market_regime}.py`
- modified: 根 `{signals,scoring,local_store,run_screener,data_fetch,market_regime}.py`（re-export）
  - `tests/test_data_quality_v2.py`、`tests/test_optimizer_asof_universe.py`
- 是否改 platform_v2.yaml: no
- 是否改 STATUS.md: no
- shared hotspot: signals / local_store（迁包核心）

## 测试证据
- `check_architecture.py --strict`: exit 0
- 全量 `pytest tests/ -q -k "not browser"`: **659 passed, 1 failed**
  （唯一失败 `test_v2_baseline_manifest::test_identity_stable_across_runs` 为既有 baseline
  采集超时，脚本不 import 被迁模块，非本波引入）
- 定向回归（数据质量/优化宇宙/逻辑平台/信号/local_store）: 39 passed

## 产物证据
- 是否使用真实 Token: no
- 是否修改 runtime 账本: no
- 是否写生产 DB PIT: no
- daily MAX: 20260818（未变）
- 旗标打印: LIVE_TRADING_ENABLED=False

## 闸门自测（G3）
- G3-1 ab_screener 正式入口: PASS（6 模块双路径 import 验证通过）
- G3-2 根文件薄 re-export: PASS（含私有名转发，旧路径 `from x import _priv` 可用）
- G3-3 离线 pytest 绿: PASS（659 passed，唯一失败为既有环境超时）

## 结论
- READY_FOR_REVIEW
- 建议总状态: BLOCKED（D/O/R 闸门仍 INSUFFICIENT，非本波范围）
