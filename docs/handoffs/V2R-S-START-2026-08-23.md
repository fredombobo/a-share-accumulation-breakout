# V2R-S Agent 启动包

## 分配

- 任务：六形态扫描、不可变信号、fill/outcome 生产接线
- 角色：signal-lifecycle-agent
- 精确 base：`7bbca60aeeaa150d133d66ebd344f5d1ee7d29fe`
- 分支：`v2r-s`
- worktree：`E:\CODEX\Stock_selection\worktrees\v2r-s`
- 最终 handoff：`docs/handoffs/V2R-S.md`

## 开始前必须执行

```powershell
$py = "E:\CODEX\Stock_selection\accumulation_breakout\.venv312\Scripts\python.exe"
Set-Location E:\CODEX\Stock_selection\worktrees\v2r-s
git status --short
git merge-base HEAD 7bbca60aeeaa150d133d66ebd344f5d1ee7d29fe
Get-Content -Raw AGENTS.md
Get-Content -Raw docs\AGENT-DISPATCH-V2-REMEDIATION-WAVE2-2026-08-23.md
```

除本启动包提交外，开始实现前工作树必须干净；merge-base 必须等于上述 SHA。

## 领取范围

仅允许修改任务板 `V2R-S.owned_paths`：strategies、signal pipeline/outcomes、signal domain/repository、legacy_scan、signal/strategy 测试和 V2R-S handoff。不得修改共享 app factory、平台配置、前端、纸面费用/风控或最终状态文件。

## 第一组 RED 测试

1. 同一 scan_run/revision 重放两次只产生一个 observation；新 revision 追加且旧记录不变。
2. EXPERIMENTAL 命中后只产生观察记录，A 池、买入草稿和目标仓位均不变化。
3. CONFIRMED/QUEUED 不进入 ENTERED；正数量 fill 才进入；零成交、拒绝、过期不进入。
4. ret_5/10/20 在对应交易日未完成或行情 `available_at` 越界时保持 NULL。

先记录修改前失败，再做最小接线。测试数据库使用 `tmp_path`；禁止读取/写入生产账本。

## 必跑验收

执行派工总表 V2R-S 的 Pytest、Ruff、Mypy 命令，并补：

```powershell
& $py -m pytest tests/test_order_risk_integration.py tests/test_execution_dual_run_integration.py -q
git diff --check
git status --short
```

## 交付协议

提交代码与 `docs/handoffs/V2R-S.md`。handoff 必须包含 base/head、owned diff、RED/GREEN、临时 DB 路径、API/schema 变化、PIT 证明、重放证明、回滚和未决项。最终只声明 `READY_FOR_REVIEW`；不得启动 O2 或改任务板。

