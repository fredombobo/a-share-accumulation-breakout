# V2R-N Agent 启动包

## 分配

- 任务：国家队/机构资金等 PIT-safe 只读信息覆盖层
- 角色：information-overlay-agent
- 精确 base：`7bbca60aeeaa150d133d66ebd344f5d1ee7d29fe`
- 分支：`v2r-n`
- worktree：`E:\CODEX\Stock_selection\worktrees\v2r-n`
- 最终 handoff：`docs/handoffs/V2R-N.md`

## 开始前必须执行

```powershell
$py = "E:\CODEX\Stock_selection\accumulation_breakout\.venv312\Scripts\python.exe"
Set-Location E:\CODEX\Stock_selection\worktrees\v2r-n
git status --short
git merge-base HEAD 7bbca60aeeaa150d133d66ebd344f5d1ee7d29fe
Get-Content -Raw AGENTS.md
Get-Content -Raw docs\AGENT-DISPATCH-V2-REMEDIATION-WAVE2-2026-08-23.md
Get-Content -Raw docs\superpowers\plans\2026-08-22-ntm-p1-overlay.md
Get-Content -Raw docs\superpowers\plans\2026-08-22-ntm-p2-wiring.md
```

## 领取范围

只修改任务板 `V2R-N.owned_paths` 的 3 个模块、1 个配置、3 个测试和 V2R-N handoff。不得修改扫描资格、评分、仓位、订单、共享配置、前端或最终状态。

## 第一组 RED 测试

1. 缺 `source`、`available_at > decision_at`、权限不足、字段缺失均返回结构化 INSUFFICIENT，且历史读取看不到未来记录。
2. adapter 使用固定离线原始响应，解析出 observation/effective/available/ingested/source/revision/confidence/evidence_refs。
3. 同一冻结行情/扫描输入，overlay on/off 后 A/B 资格、目标仓位、订单逐字段一致。
4. 供应商不可用时不伪造记录、不抛出无结构异常、不影响核心扫描闭环。

先保存修改前失败证据，再做最小实现。测试不得访问互联网；外部字段或权限不确定时必须明确 INSUFFICIENT。

## 必跑验收

执行派工总表 V2R-N 的 Pytest、Ruff、Mypy 命令，并补一条管理者可复跑的 parity fixture。配置必须有版本/hash，所有时间为带时区时间戳。

## 交付协议

提交代码与 `docs/handoffs/V2R-N.md`。handoff 包含 base/head、owned diff、RED/GREEN、数据契约、PIT 对抗用例、overlay on/off parity、权限失败、回滚和未决项。最终只声明 `READY_FOR_REVIEW`；不得把覆盖层描述成真实持仓或交易信号。
