# 全局长任务进度验收记录

日期：2026-08-30  
产品：`accumulation_breakout` / AB-Screener  
发布端口：`8001`  
结论：**PASS**

## 1. 验收范围

- 全市场扫描：服务端真实阶段、百分比、开始时间、更新时间与心跳。
- 专业回测：服务端真实阶段、百分比、消息、开始时间与心跳。
- 行情同步：数据源未提供细分百分比时使用不定进度并明确披露。
- 全局恢复：切页、窗口重新聚焦、标签页重新可见与刷新后重新读取服务端状态。
- 卡顿辨识：数值任务连续 3 分钟无状态、阶段、百分比或消息变化时给出谨慎提示。

## 2. 实现边界

- 进度百分比只来自服务端，不按耗时推算。
- “可能仍在重计算”不是失败判定，不改变任务状态。
- 进度展示不创建扫描、回测、订单或其他业务记录。
- `LIVE_TRADING_ENABLED=false`；8001 未发布纸面或真实交易 API。

## 3. 自动化证据

- 严格质量门 `scripts/quality_gate.ps1 -Strict`：Ruff、Mypy（25 个源文件）、严格架构检查、968 个 Pytest、9 个 Vitest 与生产前端构建全部通过。
- 组件测试：真实扫描百分比、疑似停滞提示、同步不定进度全部通过。
- Playwright：活动扫描跨页保持可见，`aria-valuenow=46`，390px 无横向溢出，共 6 个流程通过。
- 后端进度 I/O：持久化任务的生命周期时间与 API 映射测试通过。

## 4. 真实 8001 验收

- 健康检查：`status=ok`，行情日期 `20260828`，`stale_days=0`。
- 产品身份：`accumulation_breakout`，默认端口 8001。
- OpenAPI：共 36 条；专业回测 7 条；纸面交易 0 条。
- 只读浏览器注入活动扫描后：顶部显示 46%、阶段“资金流与形态核验”、已运行时间和最近推进时间。
- 从每日选股切到专业回测后进度仍在；390px 窄屏无溢出；浏览器 console/page error 为 0。
- 实机截图保存在本地忽略目录 `runtime/acceptance/2026-08-30/`，未写数据库。

## 5. 回滚

回滚前端全局组件挂载与对应样式，同时保留后端新增时间字段即可；这些字段是兼容性只增变更，不影响旧客户端。不得通过删除业务记录回滚。
